import React from 'react';

function CustomFormHeading({ heading }) {
  return <h2 style={{ fontFamily: 'Poppins, sans-serif', fontWeight: 700, fontSize: '3rem' }}>{heading}</h2>;

}

export default CustomFormHeading;
