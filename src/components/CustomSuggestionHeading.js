import React from 'react';

function CustomSuggestionHeading({ data }) {
  return <h2 style={{ fontFamily: 'Poppins, sans-serif', fontWeight: 400, fontSize: '1.2rem', color: '#ADADAD' }}>{data}</h2>;

}

export default CustomSuggestionHeading;
