import React from 'react';

// Creating the context object and passing the default values.
const ResumeDetailsContext = React.createContext({ dataUser: null, setUserData: () => { } });

export default ResumeDetailsContext;
